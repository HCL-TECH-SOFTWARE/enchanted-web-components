import { Meta, Title, Canvas, Controls, Stories } from '@storybook/addon-docs/blocks';
import * as EnchantedItemTypeAvatarStories from '../stories/enchanted-item-type-avatar.stories';

<Meta of={EnchantedItemTypeAvatarStories} />

<Title />

The `enchanted-item-type-avatar` displays item-specific avatar representations with customizable icons, colors, and shapes. Use in list items and data displays for visual item differentiation.

## Interactive example

<Canvas of={EnchantedItemTypeAvatarStories.Default} />

## Properties

<Controls of={EnchantedItemTypeAvatarStories.Default} />
