import { Meta, Title, Canvas, Controls, Stories } from '@storybook/addon-docs/blocks';
import * as EnchantedBreadcrumbsStories from '../stories/enchanted-breadcrumbs.stories';
import { BREADCRUMBS_PART } from '../types/cssClassEnums';

export const BREADCRUMBS_PART_METADATA = new Map([
  [BREADCRUMBS_PART.BREADCRUMBS_CONTAINER, 'Navigate container for breadcrumb trail.'],
  [BREADCRUMBS_PART.BREADCRUMBS_LIST, 'List that groups breadcrumb items.' ],
  [BREADCRUMBS_PART.BREADCRUMBS_CONTAINER, 'Navigate container for breadcrumb trail.'],
  [BREADCRUMBS_PART.BREADCRUMBS_LIST , 'List that groups breadcrumb items.'],
  [BREADCRUMBS_PART.BREADCRUMBS_ICON , 'Icon displayed in a breadcrumb item.'],
  [BREADCRUMBS_PART.BREADCRUMBS_SEPARATOR , 'Separator between breadcrumb items.'],
  [BREADCRUMBS_PART.BREADCRUMBS_ITEM , 'Breadcrumb item wrapper.'],
  [BREADCRUMBS_PART.BREADCRUMBS_ITEM_LIST , 'List item wrapper for each breadcrumb entry.'],
  [BREADCRUMBS_PART.BREADCRUMBS_DISABLED , 'Disabled breadcrumb state.'],
  [BREADCRUMBS_PART.BREADCRUMBS_ITEM_LAST , 'Last breadcrumb item styling.'],
  [BREADCRUMBS_PART.BREADCRUMBS_ITEM_LAST_ICON , 'Last item icon styling.'],
  [BREADCRUMBS_PART.BREADCRUMBS_LINK , 'Link element within a breadcrumb item.'],
  [BREADCRUMBS_PART.BREADCRUMBS_LINK_LAST , 'Last breadcrumb link styling.'],
  [BREADCRUMBS_PART.BREADCRUMBS_CONTENT , 'Content region inside a breadcrumb item.'],
  [BREADCRUMBS_PART.BREADCRUMBS_TEXT , 'Text label inside a breadcrumb item.']
]);

export const CSSPARTS = Object.values(BREADCRUMBS_PART).map((part) => ({
    name: part,
    description: BREADCRUMBS_PART_METADATA.get(part) ?? ''
}));

<Meta of={EnchantedBreadcrumbsStories} />

<Title />

The `enchanted-breadcrumbs` component displays navigation path hierarchy with optional icons and disabled states. Use breadcrumbs for showing current location in site structure and enabling quick navigation to parent levels.

## Interactive example

<Canvas of={EnchantedBreadcrumbsStories.EnchantedBreadcrumbs} />

## Properties

<Controls of={EnchantedBreadcrumbsStories.EnchantedBreadcrumbs} />

## CSS Parts

<table>
 <thead>
   <tr>
     <th> Part </th>
     <th> Description </th>
    </tr>
 </thead>
 <tbody>
  {CSSPARTS.map((part) => (
    <tr key={part.name}>
      <td><code>{part.name}</code></td>
      <td>{part.description}</td>
    </tr>
  ))}
  </tbody>
</table>

## Slots

This component does not expose any slots.