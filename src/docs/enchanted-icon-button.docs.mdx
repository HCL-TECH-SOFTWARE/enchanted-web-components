import { Meta, Title, Canvas, Controls, Stories } from '@storybook/addon-docs/blocks';
import * as EnchantedIconButtonStories from '../stories/enchanted-icon-button.stories';

<Meta of={EnchantedIconButtonStories} />

<Title />

The `enchanted-icon-button` is a compact icon-only button component supporting three sizes (small, medium, FAB) with optional padding and inverse color schemes. Use for toolbar actions, quick access features, or floating action buttons.

## Interactive example

<Canvas of={EnchantedIconButtonStories.Default} />

## Properties

<Controls of={EnchantedIconButtonStories.Default} />

